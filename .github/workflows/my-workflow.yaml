name: hello world demo

on:
 push:
  branches:
  -     main

jobs:
say-hello:
     run-on: ubantu-latest
  steps:
   -  name: check out code
      use: action/checkout@v4

   -  name: print hello
      run: echo"hello , Github Action"

      
